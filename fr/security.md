---
title: "Sécurité et mode natif"
description: "Le mode sûr démarre avec HostAccess.NONE. Gardez 127.0.0.1, l’authentification et le minimum de capacités ; n’accordez jamais native.java/native.paper à du code non fiable."
icon: shield-check
---

Native Mode must be enabled in both `config.yml` and `workspace.json`. Treat a native workspace exactly like a Java plugin and review all of its source before enabling it.

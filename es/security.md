---
title: "Seguridad y Modo Nativo"
description: "El Modo Seguro comienza con HostAccess.NONE. Conserva 127.0.0.1, autenticación activa y capacidades mínimas; nunca otorgues native.java o native.paper a código no confiable."
icon: shield-check
---

Native Mode must be enabled in both `config.yml` and `workspace.json`. Treat a native workspace exactly like a Java plugin and review all of its source before enabling it.

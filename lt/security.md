---
title: "Sauga ir Native Mode"
description: "Safe Mode prasideda su HostAccess.NONE. Palikite 127.0.0.1, autentifikavimą ir minimalias teises; neduokite native.java/native.paper nepatikimam kodui."
icon: shield-check
---

Native Mode must be enabled in both `config.yml` and `workspace.json`. Treat a native workspace exactly like a Java plugin and review all of its source before enabling it.

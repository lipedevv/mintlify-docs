---
title: "Bezpečnost a Native Mode"
description: "Safe Mode začíná s HostAccess.NONE. Zachovejte 127.0.0.1, autentizaci a minimum oprávnění; nedávejte native.java/native.paper nedůvěryhodnému kódu."
icon: shield-check
---

Native Mode must be enabled in both `config.yml` and `workspace.json`. Treat a native workspace exactly like a Java plugin and review all of its source before enabling it.

---
title: "Bezpečnosť a Native Mode"
description: "Safe Mode začína s HostAccess.NONE. Zachovajte 127.0.0.1, autentifikáciu a minimum oprávnení; nedávajte native.java/native.paper nedôveryhodnému kódu."
icon: shield-check
---

Native Mode must be enabled in both `config.yml` and `workspace.json`. Treat a native workspace exactly like a Java plugin and review all of its source before enabling it.

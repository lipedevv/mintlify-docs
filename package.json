{
  "name": "nexora-documentation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mint dev --no-open",
    "validate": "mint validate",
    "check:links": "mint broken-links --check-anchors --check-redirects",
    "check:a11y": "mint a11y"
  },
  "devDependencies": {
    "mint": "4.2.819"
  },
  "engines": {
    "node": ">=20.17.0"
  }
}

---
title: "Security and Native Mode"
description: "Safe Mode starts with HostAccess.NONE. Keep the API bound to 127.0.0.1, keep authentication enabled, review all capabilities, and never grant native.java or native.paper to…"
icon: shield-check
---

Native Mode must be enabled in both `config.yml` and `workspace.json`. Treat a native workspace exactly like a Java plugin and review all of its source before enabling it.
